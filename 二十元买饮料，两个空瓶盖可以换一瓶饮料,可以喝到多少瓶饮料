#include<stdio.h>
int main()
{
	int total = 0;
	int money = 0;
	scanf("%d", &money);
	//买回来的汽水喝掉
	total = money;
	int empty = 0;
	empty = money;
	//换回来的汽水
	while (empty >=2)
	{
		total += empty / 2;
		empty = empty / 2 + empty % 2;
	}
	printf("%d ", total);
	return 0;
}
