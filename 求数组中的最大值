#include<stdio.h>
int main()
{
	int max = 0;
	int arr[] = { 23,45,5646,758,45,234,9999,745 };
	max = arr[0];
	int sz = sizeof(arr) / sizeof(arr[0]);
	for (int i = 0; i < sz; i++)
	{
		if (arr[i] > max)
		{
			max = arr[i];
		}
	}
	printf("这个数组中的最大值为:%d", max);
	return 0;
}
