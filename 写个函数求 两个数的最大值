
#include<stdio.h>
int get_max(int x, int y)
{
	return x > y ? x : y;
}
int main()
{
	int a, b;
	printf("请输入两个数>;");
	scanf("%d%d", &a, &b);
	int max = get_max(a, b);
	printf("max=%d", max);
	return 0;
}
