#include<stdio.h>

void swap(int* x, int* y)
{
	int tmp = 0;
	tmp = *x;
	 *x = *y;
	 *y = tmp;
}
int main()
{
	int a=0;
	int b = 0;
	printf("请输入两个数>;");
	scanf("%d%d", &a, &b);
	printf("a=%d,b=%d\n", a, b);
	swap(&a, &b);
	printf("a=%d,b=%d\n", a, b);
	return 0;
}
